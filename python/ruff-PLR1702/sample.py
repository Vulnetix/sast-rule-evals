def deeply_nested(data):
    if data:
        for item in data:
            if item:
                for sub in item:
                    if sub:
                        while sub:
                            if True:
                                pass
