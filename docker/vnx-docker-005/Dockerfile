# vnx-docker-005 eval target: privileged container flag
FROM ubuntu:22.04

RUN apt-get update && apt-get install -y curl

# TRIGGERS rule (--privileged in a run context)
# This is referenced in docker-compose or run command patterns
CMD ["bash"]
